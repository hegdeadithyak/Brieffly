import CoursesSection from "./components/Card/CourseSection"; // Corrected import path
// import './courses.css'; // Import the CSS file for styling

export default function Courses() { // Capitalized function name to follow convention
  return (
    <div> {/* Apply a class for styling */}
        {/* Add the CourseSection here */}
        <CoursesSection />
    </div>
  );
}